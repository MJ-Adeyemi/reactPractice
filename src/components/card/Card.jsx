import React from 'react'

const Card = () => {
    const data = [
      { name: "Ade", age: 12, isPresent: true, school: "OIC" },
      { name: "Ade", age: 12, isPresent: true, school: "OIC" },
      { name: "Ade", age: 12, isPresent: true, school: "OIC" },
    ];
  return (
    <div>
      {data.map((item,index)=>{
        
      })}
    </div>
  )
}

export default Card
